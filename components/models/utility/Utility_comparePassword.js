  
    
    /**
     * @memberof Utility
     * @method comparePassword(password)
     * 			@public
     *  			Esse metodo pode ser usado por todos.
     * 			@param {String} password
     * 			@description
     * 				Esse metodo verifica se o parametro password é igual
     * 				ao input de confirmação, se existir.
     * 			@return {Boolean}
     * 				Retorna true se for igual, e falso se não for.
     */

    comparePassword( form ) {

        
        return week1 <= week2;
    }